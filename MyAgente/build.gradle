dependencies {
    compile project(':Architettura')
    //compile fileTree(dir: '../Documents/Java/lib/aima', include: 'aima-core.jar')
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
        resources {
            srcDirs = ['src']
        }
    }
}

task runAgent(type: JavaExec, dependsOn: build) {
    doFirst{
        println "Starting MyAgent..."
    }

    classpath = sourceSets.main.runtimeClasspath
    main = 'it.jar.mulino.Main'

    // parametri
    // args 60
}
